# セキュリティについて

このツールは **構内交換機 (PBX) の設定を丸ごと書き換えられます**。
画面に到達できる人は、内線やトランクの設定変更、留守番電話の録音の再生・
ダウンロード、発信ルートの書き換え (= 外線発信のなりすまし) が行えます。
一般的な Web アプリよりも慎重な設置をお願いします。

## 設置時に必ず行うこと

### 1. 管理パスワードを設定する

`.env` に `ADMIN_PASSWORD` を設定してください。設定しない限り、
**ログイン画面は出ません**。未設定のときは起動ログと画面上部に警告が出ます。

```bash
# 推測されにくいパスワードを作る
head -c 18 /dev/urandom | base64

# .env に書く
ADMIN_USER=admin
ADMIN_PASSWORD=<上で作った文字列>
```

平文を置きたくない場合はハッシュで設定できます。

```bash
python -m app.auth hash
# 出力された ADMIN_PASSWORD_HASH=... の行を .env に貼る
```

### 2. 待ち受けアドレスを絞る

既定は `HOST=127.0.0.1` (サーバー内からのみ) です。
LAN から使う場合だけ `0.0.0.0` にし、その際は必ず `ADMIN_PASSWORD` を
設定してください。

### 3. インターネットに直接出さない

この画面をグローバル IP やポート開放で公開しないでください。
社外から使う必要がある場合は VPN を経由させてください。

### 4. `DEBUG=false` にする

`true` のままだと例外発生時にスタックトレース (設定内容・パスを含む) が
ブラウザに返ります。

### 5. `AMI_SECRET` を変更する

`manager.conf` に書き出されます。既定値のままにしないでください。

### 6. `.env` と `pbx.db` を Git に入れない

`.gitignore` で除外していますが、バックアップ zip (`backups/`) にも
`.env` と DB が含まれます。取り扱いに注意してください。

## 本ツールが行っている対策

| 対策 | 内容 |
|---|---|
| 設定ファイルインジェクション | DB へ書き込む直前に改行・制御文字を除去 (`app/models/base.py`)。さらに生成時にもコンテキスト名・ダイヤル先・パターンを安全な文字種へ正規化 (`app/services/asterisk_config.py`) |
| ログイン | pbkdf2-sha256 (24 万回) でのハッシュ照合、`hmac.compare_digest` による定数時間比較、IP ごとの試行回数制限、ログイン成功時のセッション ID 再生成 |
| CSRF | セッション Cookie を `SameSite=Lax` にして、外部サイトからの POST では Cookie が送られないようにしている |
| XSS | Jinja2 の自動エスケープに加え、JavaScript 側も `innerHTML` の文字列連結をやめて DOM API で組み立て |
| 内部フック | Asterisk からの通知はトークン (初回起動時に自動生成) と送信元 IP の両方で検証。受信 FAX のパスは FAX スプール配下に限定 |
| 外部依存 | htmx を同梱し、外部 CDN からスクリプトを読み込まない (閉じた LAN でも動き、配信内容の差し替えも受けない) |
| ヘッダー | `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy` を付与 |

## 想定していないこと

- **多要素認証・複数ユーザー・権限分け**はありません。管理者 1 アカウントのみです。
- **監査ログ**はありません (操作の記録は残りません)。
- **HTTPS 終端**は行いません。TLS が必要な場合は nginx 等を前段に置いてください。

社内 LAN に置いた業務用 PBX の管理ツール、という前提での設計です。
これより厳しい要件がある環境では、前段にリバースプロキシを置いて
認証・TLS・アクセス制限を追加してください。

## 脆弱性の報告

問題を見つけた場合は、公開 Issue ではなく GitHub の
Security Advisory (Security タブ → Report a vulnerability) からご連絡ください。
